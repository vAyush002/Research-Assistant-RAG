# Smart Research Assistant — pinned to known-good versions.
# Install:  pip install -r requirements.txt

# --- Web UI ---
streamlit==1.58.0

# --- RAG / LLM ---
# Do NOT pin langchain-core — let pip resolve it to a version that satisfies
# every langchain-* package below (they require different core minimums).
langchain==1.3.4
langchain-community==0.4.2
langchain-text-splitters==1.1.2

# LLM providers (multi-provider sidebar: Groq / Claude / OpenAI)
langchain-groq==1.1.2
langchain-anthropic==1.4.6
langchain-openai==1.3.2

# --- Vector store & embeddings ---
# Vector store is an in-memory NumPy cosine-similarity index (see vector_store.py).
# ChromaDB was removed: it pulls in opentelemetry/grpc/protobuf, which crashes on
# import under some Python builds (protobuf "Descriptors cannot be created directly").
numpy>=1.26
sentence-transformers==5.5.1

# --- Document parsing & utils ---
pypdf==6.12.2
python-dotenv==1.2.2

# --- Optional: FastAPI backend (main.py). Not needed for the Streamlit app. ---
fastapi==0.136.3
uvicorn==0.48.0
python-multipart==0.0.30
requests==2.34.2
